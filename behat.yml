default:
    suites:
        default:
            paths:
                - '%paths.base%/tests/features/'
            contexts:
                - DocumentationContext
                - FeatureFilesContext
